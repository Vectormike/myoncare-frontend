# myoncare

### Prerequisites

1. `NodeJs`
2. `NPM`
3. `React`
4. `Docker`

## Project setup

Run the server:

```
npm start
```

### Docker

To run with Docker:

```
docker-compose build
```

```
docker-compose up -d
```

The port exposed is `3000`
